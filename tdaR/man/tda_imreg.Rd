% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interval.R
\name{tda_imreg}
\alias{tda_imreg}
\title{Conditional means with interval-valued data}
\usage{
tda_imreg(formula, data, options = list(), dir = tempfile("tda"), ...)
}
\arguments{
\item{formula}{a formula using \code{iv()} for interval-valued variables;
\code{tda_imreg} needs both sides interval-valued.}

\item{data}{a data frame.}

\item{options}{a named list of further TDA options, passed through.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
An object carrying \code{table}, one row per partition boundary.
}
\description{
\code{imreg}: for each interval formed by the induced partition of the
regressor's bounds, the mean of the response bounds over the cases
covering it.
}
\examples{
set.seed(3)
d <- data.frame(xlo = round(rnorm(30), 2))
d$xhi <- d$xlo + round(runif(30, 0.3, 1), 2)
d$ylo <- 2 + 0.5 * d$xlo + rnorm(30, sd = 0.2)
d$yhi <- d$ylo + round(runif(30, 0.3, 1), 2)
head(tda_imreg(iv(ylo, yhi) ~ iv(xlo, xhi), d)$table)
}
\seealso{
Other interval-valued data:
\code{\link[=tda_bounds]{tda_bounds()}},
\code{\link[=tda_idf]{tda_idf()}},
\code{\link[=tda_ilsreg]{tda_ilsreg()}},
\code{\link[=tda_imean]{tda_imean()}},
\code{\link[=tda_inpreg]{tda_inpreg()}},
\code{\link[=tda_ivar1]{tda_ivar1()}},
\code{\link[=tda_ivreg]{tda_ivreg()}},
\code{\link[=tda_sddf]{tda_sddf()}}
}
\concept{interval-valued data}
