% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-analysis.R
\name{tda_gdu}
\alias{tda_gdu}
\title{Union of two graphs}
\usage{
tda_gdu(edges1, edges2, missing_value = -1, ...)
}
\arguments{
\item{edges1, edges2}{data frames: from, to, value.}

\item{missing_value}{substitute written for missing values.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
the output file's lines.
}
\description{
TDA's \code{gdu}: graph 1 is loaded from \code{edges1} (with the
\code{perm} option of \code{gdd}), then the union with the edge
list \code{edges2} is written to the output file.
}
\examples{
e1 <- data.frame(i = c(1, 2), j = c(2, 3), v = c(1, 1))
e2 <- data.frame(i = c(1, 3), j = c(3, 1), v = c(2, 2))
tda_gdu(e1, e2)   # value1/value2 side by side, -1 where absent
}
