% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/episodes.R
\name{tda_episodes}
\alias{tda_episodes}
\alias{tda_state_dist}
\title{The episode data behind a model}
\usage{
tda_episodes(
  formula,
  data,
  vars = NULL,
  options = list(),
  dir = tempfile("tda"),
  ...
)

tda_state_dist(
  formula,
  data,
  times,
  id = NULL,
  spell = NULL,
  options = list(),
  dir = tempfile("tda"),
  ...
)
}
\arguments{
\item{formula}{the same \code{Surv()} formula the model functions take.}

\item{data}{a data frame.}

\item{vars}{additional variables to carry into the output, as names.}

\item{options}{a named list of further TDA options, passed through.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}

\item{times}{time points at which to evaluate the state distribution.}

\item{id, spell}{for \code{tda_state_dist}, columns identifying the case
and numbering its spells, for multi-episode data (\code{edef}'s
\code{id=} and \code{sn=}).}
}
\value{
A data frame of episodes, or of state proportions by time.
}
\description{
\code{tda_episodes} returns the episode data \code{edef} builds from a
formula, which is what every model in this package is actually fitted to.
It is the way to check that a \code{Surv()} specification means what you
intended before trusting a fit.
}
\details{
\code{tda_state_dist} gives the distribution over states at a set of time
points -- how many cases occupy each state when -- which is the natural
summary of multi-state data.
}
\examples{
d <- data.frame(t = c(4, 3, 1, 5), s = c(1, 0, 1, 1), x = c(2, 1, 3, 1))
tda_episodes(Surv(t, s) ~ x, d)
tda_state_dist(Surv(t, s) ~ x, d, times = c(1, 3, 5))
}
\seealso{
Other episodes:
\code{\link[=tda_esort]{tda_esort()}}
}
\concept{episodes}
