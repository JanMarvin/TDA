% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize.R
\name{tda_boolean_min}
\alias{tda_boolean_min}
\title{Boolean function minimization}
\usage{
tda_boolean_min(
  y,
  conditions,
  data = NULL,
  undefined = c("dont_care", "true", "false"),
  algorithm = c("lawler", "petrick", "lawler2", "none"),
  max_implicants = NULL,
  options = list(),
  dir = tempfile("tda"),
  ...
)
}
\arguments{
\item{y}{the outcome: a 0/1 vector, or a column name in \code{data}.}

\item{conditions}{the condition variables: a 0/1 matrix or data
frame, or column names in \code{data} (at most 15).}

\item{data}{optional data frame supplying the columns.}

\item{undefined}{how rows never observed are treated:
\code{"dont_care"} (default), \code{"true"}, or \code{"false"}.}

\item{algorithm}{cover selection: \code{"lawler"} (default),
\code{"petrick"}, \code{"lawler2"}, or \code{"none"} for prime
implicants only.}

\item{max_implicants}{storage cap.}

\item{options}{a named list of further TDA options for \code{bfa}
(\code{ptab}, \code{prot}, ...).}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
A list: \code{$selections}, a data frame with one row per
  term of each minimal cover -- \code{selection} number and one
  column per condition holding 1, 0, or NA for "does not matter" --
  and \code{$expression}, the printed symbolic forms.
}
\description{
Which combinations of binary conditions produce the outcome? TDA's
\code{bfa} builds the truth table of \code{y} over the condition
variables, finds all prime implicants (Quine-McCluskey), and selects
minimal covers. This is the machinery behind crisp-set QCA-style
analyses.
}
\examples{
d <- expand.grid(X1 = 0:1, X2 = 0:1, X3 = 0:1)
d$Y <- as.integer(d$X1 & d$X2 | d$X3)
tda_boolean_min("Y", c("X1", "X2", "X3"), data = d)$selections
}
\seealso{
Other optimization:
\code{\link[=tda_mlp]{tda_mlp()}},
\code{\link[=tda_mlpi]{tda_mlpi()}},
\code{\link[=tda_mls]{tda_mls()}},
\code{\link[=tda_mqap]{tda_mqap()}},
\code{\link[=tda_mqp]{tda_mqp()}}
}
\concept{optimization}
