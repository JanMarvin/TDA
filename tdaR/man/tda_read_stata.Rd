% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{tda_read_stata}
\alias{tda_read_stata}
\title{Read a Stata file}
\usage{
tda_read_stata(
  file,
  n_records = NULL,
  missing = NULL,
  upper_names = FALSE,
  options = list(),
  dir = tempfile("tda"),
  ...
)
}
\arguments{
\item{file}{path to the \code{.dta} file.}

\item{n_records}{number of records to read. \code{NULL}, the default,
reads all of them.}

\item{missing}{value to substitute for Stata's system missing.
TDA's default is -5.}

\item{upper_names}{translate variable names to upper case
(\code{rstata}'s \code{n=2}). Default \code{FALSE}.}

\item{options}{a named list of further TDA options, passed through.
The reader also accepts, through \code{options}, TDA's
\code{noc=}, \code{msys=}, \code{arcd=}, \code{dfa=},
\code{fn=}, \code{p=}, \code{pn=}, \code{zoo=} and
\code{vdf=} switches.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
A data frame. \code{attr(x, "run")} carries the run.
}
\description{
Reads a Stata \code{.dta} file through TDA's \code{rstata}, which
supports releases 4, 6, 7 and 10 -- the same set
\code{\link{tda_write_stata}} writes, so a file written here reads
back here.
}
\examples{
d <- data.frame(id = 1:4, y = c(1.5, 2, 2.5, 3))
f <- tempfile(fileext = ".dta")
tda_write_stata(d, f)
tda_read_stata(f)
}
\seealso{
\code{\link{tda_write_stata}}, \code{\link{tda_read_spss}}
}
