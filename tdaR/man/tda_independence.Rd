% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptive.R
\name{tda_independence}
\alias{tda_independence}
\title{Delta-independence of two integer variables}
\usage{
tda_independence(
  x,
  y,
  data = NULL,
  partition = NULL,
  dir = tempfile("tda"),
  ...
)
}
\arguments{
\item{x, y}{integer-valued vectors of equal length, or column names in
\code{data}.}

\item{data}{optional data frame supplying \code{x} and \code{y}.}

\item{partition}{optional increasing integers: the upper ends of
consecutive \code{y} subsets.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
A data frame with one row per subset: \code{subset} (its
  number), \code{values} (the \code{y} values it contains, as a
  comma-separated string), and \code{delta}.
}
\description{
How far two integer-valued variables are from independent: for each
subset of \code{y}'s values, the largest gap between the distribution
of \code{x} within that subset and the overall distribution of
\code{x} -- TDA's \code{indep}. A delta of 0 means \code{x} is
distributed identically inside and outside the subset.
}
\details{
By default every distinct value of \code{y} is its subset. With
\code{partition}, \code{y}'s range is cut into consecutive subsets:
each number is the last \code{y} value of one subset (TDA's
\code{y=}). Values must be integers; TDA works on the integer grid
from the smallest to the largest observed value.
}
\examples{
d <- data.frame(x = c(1, 2, 3, 4, 1, 2, 3, 4),
                y = c(1, 1, 1, 1, 2, 2, 2, 2))
tda_independence("x", "y", data = d)
}
\seealso{
Other descriptive statistics:
\code{\link[=tda_brr]{tda_brr()}},
\code{\link[=tda_cov]{tda_cov()}},
\code{\link[=tda_dstat]{tda_dstat()}},
\code{\link[=tda_freq]{tda_freq()}},
\code{\link[=tda_ineq]{tda_ineq()}},
\code{\link[=tda_loglin]{tda_loglin()}},
\code{\link[=tda_quant]{tda_quant()}},
\code{\link[=tda_rcorr]{tda_rcorr()}},
\code{\link[=tda_segr]{tda_segr()}},
\code{\link[=tda_subm]{tda_subm()}}
}
\concept{descriptive statistics}
