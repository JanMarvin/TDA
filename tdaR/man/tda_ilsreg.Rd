% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interval.R
\name{tda_ilsreg}
\alias{tda_ilsreg}
\title{Least squares with an interval-valued response}
\usage{
tda_ilsreg(
  formula,
  data,
  yl,
  censor,
  options = list(),
  dir = tempfile("tda"),
  ...
)
}
\arguments{
\item{formula}{a formula using \code{iv()} for the interval-valued
response, e.g. \code{iv(ylo, yhi) ~ x}.}

\item{data}{a data frame.}

\item{yl, censor}{the response and censoring variables TDA requires, as names
or vectors.}

\item{options}{a named list of further TDA options, passed through.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
An object of class \code{tda_fit}.
}
\description{
The response is interval-valued, the regressor a point value -- unlike
\code{\link{tda_ivreg}}, where both sides are intervals. Both the slope
and the intercept come back as bounds; see \code{\link{tda_bounds}}.
}
\examples{
set.seed(2)
d <- data.frame(x = round(rnorm(30), 2))
d$ylo <- 2 + 0.5 * d$x + rnorm(30, sd = 0.2)
d$yhi <- d$ylo + round(runif(30, 0.3, 1), 2)
tda_ilsreg(iv(ylo, yhi) ~ x, d)
}
\seealso{
Other interval-valued data:
\code{\link[=tda_bounds]{tda_bounds()}},
\code{\link[=tda_idf]{tda_idf()}},
\code{\link[=tda_imean]{tda_imean()}},
\code{\link[=tda_imreg]{tda_imreg()}},
\code{\link[=tda_inpreg]{tda_inpreg()}},
\code{\link[=tda_ivar1]{tda_ivar1()}},
\code{\link[=tda_ivreg]{tda_ivreg()}},
\code{\link[=tda_sddf]{tda_sddf()}}
}
\concept{interval-valued data}
