% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix.R
\name{tda_mrsum}
\alias{tda_mrsum}
\alias{tda_mcsum}
\alias{tda_mdrow}
\alias{tda_mdcol}
\title{Row and column sums, as vectors or diagonal matrices}
\usage{
tda_mrsum(A, dir = tempfile("tda"))

tda_mcsum(A, dir = tempfile("tda"))

tda_mdrow(A, dir = tempfile("tda"))

tda_mdcol(A, dir = tempfile("tda"))
}
\arguments{
\item{A}{a numeric matrix.}

\item{dir}{working directory.}
}
\value{
\code{tda_mrsum}/\code{tda_mcsum} return a plain numeric
  vector; \code{tda_mdrow}/\code{tda_mdcol} return a diagonal
  matrix.
}
\description{
\code{tda_mrsum}/\code{tda_mcsum} are TDA's \code{mrsum}/
\code{mcsum}, matching base R's \code{rowSums}/\code{colSums}.
\code{tda_mdrow}/\code{tda_mdcol} return the same sums as a
diagonal matrix instead of a vector -- TDA's \code{mdrow}/
\code{mdcol}, matching \code{diag(rowSums(A))}/
\code{diag(colSums(A))}; useful for building a graph's degree
matrix from its adjacency matrix.
}
\examples{
A <- matrix(1:6, 2, 3)
tda_mrsum(A); tda_mcsum(A)
tda_mdrow(A); tda_mdcol(A)
}
\seealso{
Other matrix reshaping:
\code{\link[=tda_mag]{tda_mag()}},
\code{\link[=tda_mcath]{tda_mcath()}},
\code{\link[=tda_mcvec]{tda_mcvec()}},
\code{\link[=tda_msort]{tda_msort()}},
\code{\link[=tda_msrow]{tda_msrow()}},
\code{\link[=tda_mtrim]{tda_mtrim()}}
}
\concept{matrix reshaping}
