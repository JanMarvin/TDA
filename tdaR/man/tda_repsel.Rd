% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{tda_repsel}
\alias{tda_repsel}
\title{Repeat or select block cases}
\usage{
tda_repsel(data, by, times, commands = character(), ...)
}
\arguments{
\item{data}{data frame.}

\item{by}{block-defining column.}

\item{times}{matrix expression (TDA syntax) of dimension blocks x 1.}

\item{commands}{further command lines run under the replicated
case structure. Column names are TDA-sanitised (upper-cased)
first, so refer to them in that form.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
the printed output, invisibly.
}
\description{
TDA's \code{repsel} with a matrix expression giving, per block,
the repetition count.
}
\examples{
# blocks by G, block 1 twice and block 2 once: cases become
# X = 1,2,1,2,3, and the statistics confirm it (mean 1.8, sum 9)
out <- tda_repsel(data.frame(G = c(1, 1, 2), X = 1:3), by = "G",
                  times = "<2, 1>", commands = "dstat = X;")
grep("^X", out, value = TRUE)
}
