% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{tda_expressions}
\alias{tda_expressions}
\alias{tda_expand}
\alias{tda_mparse}
\title{Expand an expression, or parse a matrix expression}
\usage{
tda_expand(expr, setup = NULL, options = list(), dir = tempfile("tda"), ...)

tda_mparse(
  expr,
  setup = NULL,
  mfmt = NULL,
  options = list(),
  dir = tempfile("tda"),
  ...
)
}
\arguments{
\item{expr}{the expression, as a single string.}

\item{setup}{TDA commands to run first, as a character vector. A
matrix expression needs its matrices to exist, so this is where
\code{mdef}/\code{mdefi} lines go.}

\item{options}{a named list of further TDA options, passed through.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}

\item{mfmt}{for \code{tda_mparse}: print format, TDA's default is
\code{12.4}.}
}
\value{
A character vector: the lines TDA printed in response.
  \code{attr(x, "run")} carries the run.
}
\description{
\code{tda_expand} runs TDA's \code{expm}, which expands an expression
into its fully written-out form -- a model formula with interaction
shorthand becomes the explicit list of terms. \code{tda_mparse} runs
\code{mparse}, which parses a matrix expression and reports how it
read it, without evaluating anything.
}
\details{
Both are diagnostics: they answer "what did TDA make of what I
wrote", which is otherwise only visible in an error message.
}
\examples{
tda_expand("A*B")
tda_mparse("A+B", setup = c("mdefi(2,2,A);", "mdefi(2,2,B);"))
}
