% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix.R
\name{tda_mag}
\alias{tda_mag}
\title{Aggregating a matrix by row and column groups}
\usage{
tda_mag(A, row_groups, col_groups, dir = tempfile("tda"))
}
\arguments{
\item{A}{a numeric matrix.}

\item{row_groups}{integer group index for each row, length
\code{nrow(A)}.}

\item{col_groups}{integer group index for each column, length
\code{ncol(A)}.}

\item{dir}{working directory.}
}
\value{
A numeric matrix.
}
\description{
Sums \code{A}'s entries within each (row-group, column-group)
block -- TDA's \code{mag}. \code{row_groups}/\code{col_groups}
assign each row/column of \code{A} to a group (any positive
integers; groups need not be contiguous or start at 1). The
result has one row per distinct value in \code{row_groups} and
one column per distinct value in \code{col_groups}, in ascending
order of group number.
}
\examples{
A <- matrix(1:12, 3, 4, byrow = TRUE)
tda_mag(A, row_groups = c(1, 2, 1), col_groups = c(1, 1, 2, 2))
}
\seealso{
Other matrix reshaping:
\code{\link[=tda_mcath]{tda_mcath()}},
\code{\link[=tda_mcvec]{tda_mcvec()}},
\code{\link[=tda_mrsum]{tda_mrsum()}},
\code{\link[=tda_msort]{tda_msort()}},
\code{\link[=tda_msrow]{tda_msrow()}},
\code{\link[=tda_mtrim]{tda_mtrim()}}
}
\concept{matrix reshaping}
