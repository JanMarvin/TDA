% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotcmds.R
\name{tda_pl_arc}
\alias{tda_pl_arc}
\title{Curved arcs}
\usage{
tda_pl_arc(p, x, y, curvature = 0, arrow = NULL, lty = NULL, lw = NULL, ...)
}
\arguments{
\item{p}{a \code{tda_ps} session.}

\item{x, y}{coordinates the arcs connect, in order; at least two points.}

\item{curvature}{how much the arc bows out, as a fraction of the
distance between the two points; \code{0} is a straight line. TDA's
own \code{sc=}.}

\item{arrow}{size of an arrowhead at the end of each arc, as
\code{c(length, width)} in mm; no arrowhead if not given.}

\item{lty, lw}{line type and line width in mm for the arc.}

\item{...}{further options for the command.}
}
\value{
The session, with the command added.
}
\description{
\code{plotk}: draws an arc, optionally ending in an arrow, between
each consecutive pair of points -- a curved alternative to
\code{\link{tda_pl_lines}} for showing a directed relationship without
the line hiding what is under it. Fixed along the way: a genuine
rendering bug where a shallow arc (TDA's technique for one -- a
small slice of a large-radius circle) was measured by the full
circle's radius when sizing the plot, inflating the bounding box to
fit an almost entirely off-screen circle and shrinking everything
else into a corner; the fix (using the arc segment's actual
extent) is general, not specific to this function.
}
\examples{
p <- tda_ps(xlim = c(0, 10), ylim = c(0, 10))
p <- tda_pl_frame(p)
p <- tda_pl_arc(p, c(2, 8), c(2, 8), curvature = 3, arrow = c(1.5, 1))
plot(p)
}
\seealso{
Other plotting:
\code{\link[=plot.tda_ple]{plot.tda_ple()}},
\code{\link[=tda_check_ps]{tda_check_ps()}},
\code{\link[=tda_pl]{tda_pl()}},
\code{\link[=tda_pl_axis]{tda_pl_axis()}},
\code{\link[=tda_pl_graph]{tda_pl_graph()}},
\code{\link{tda_pl_hist}},
\code{\link[=tda_pl_panel]{tda_pl_panel()}},
\code{\link[=tda_pl_regression]{tda_pl_regression()}},
\code{\link[=tda_pl_scatter]{tda_pl_scatter()}},
\code{\link[=tda_plot_ps]{tda_plot_ps()}},
\code{\link[=tda_ps]{tda_ps()}},
\code{\link[=tda_ps3]{tda_ps3()}},
\code{\link[=tda_read_ps]{tda_read_ps()}}
}
\concept{plotting}
