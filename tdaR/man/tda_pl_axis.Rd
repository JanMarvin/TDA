% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotcmds.R
\name{tda_pl_axis}
\alias{tda_pl_axis}
\title{Draw a single axis, optionally at an explicit position}
\usage{
tda_pl_axis(
  p,
  which = c("x", "y"),
  sc = NULL,
  ic = NULL,
  lty = NULL,
  lw = NULL,
  fs = NULL,
  tl = NULL,
  fmt = NULL,
  at = NULL,
  dir = NULL,
  ...
)
}
\arguments{
\item{p}{a \code{\link{tda_ps}} session.}

\item{which}{\code{"x"} or \code{"y"}, which axis to draw.}

\item{sc, ic, lty, lw, fs, tl, fmt}{as in \code{\link{tda_pl_axes}}, for this
one axis.}

\item{at}{the axis line's endpoints, \code{c(xa, ya, xb, yb)} in data
units, when it is not simply the plot's boundary (TDA's
\code{plxa(...) = xa,ya,xb,yb} right-hand side). Omit for an ordinary
axis at the plot boundary.}

\item{dir}{which side of the axis line the tick labels are written on
(TDA's \code{dir=}); only meaningful together with \code{at}.}

\item{...}{further options for \code{plxa}/\code{plya}.}
}
\value{
The session, with the command added.
}
\description{
\code{tda_pl_axes} draws both axes together, at the plot's boundary.
\code{tda_pl_axis} draws one axis at a time and, via \code{at}, can place
it anywhere in the plot rather than only at the boundary -- TDA's
technique for a second x or y axis (e.g. a top axis in different units
from the bottom one), which \code{tda_pl_axes} alone cannot reach.
}
\examples{
p <- tda_ps(xlim = c(-2, 2), ylim = c(0, 1))
p <- tda_pl_axis(p, "x", sc = 1, ic = 2)
p <- tda_pl_axis(p, "y", sc = 0.5, ic = 0)
# a second x axis, drawn at y = 1 instead of the plot's bottom edge
p <- tda_pl_axis(p, "x", sc = 1, ic = 5, dir = 1, at = c(-1, 1, 1, 1))
}
\seealso{
Other plotting:
\code{\link[=plot.tda_ple]{plot.tda_ple()}},
\code{\link[=tda_check_ps]{tda_check_ps()}},
\code{\link[=tda_pl]{tda_pl()}},
\code{\link[=tda_pl_arc]{tda_pl_arc()}},
\code{\link[=tda_pl_graph]{tda_pl_graph()}},
\code{\link{tda_pl_hist}},
\code{\link[=tda_pl_panel]{tda_pl_panel()}},
\code{\link[=tda_pl_regression]{tda_pl_regression()}},
\code{\link[=tda_pl_scatter]{tda_pl_scatter()}},
\code{\link[=tda_plot_ps]{tda_plot_ps()}},
\code{\link[=tda_ps]{tda_ps()}},
\code{\link[=tda_ps3]{tda_ps3()}},
\code{\link[=tda_read_ps]{tda_read_ps()}}
}
\concept{plotting}
