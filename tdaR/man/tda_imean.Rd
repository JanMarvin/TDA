% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interval.R
\name{tda_imean}
\alias{tda_imean}
\alias{tda_ivariance}
\alias{tda_igini}
\title{Statistics of an interval-valued variable}
\usage{
tda_imean(formula, data, options = list(), dir = tempfile("tda"), ...)

tda_ivariance(
  formula,
  data,
  max_boxes = NULL,
  max_iter = NULL,
  tol_width = NULL,
  tol_fd = NULL,
  tol_fe = NULL,
  options = list(),
  dir = tempfile("tda"),
  ...
)

tda_igini(
  formula,
  data,
  x = NULL,
  options = list(),
  dir = tempfile("tda"),
  ...
)
}
\arguments{
\item{formula}{a one-sided formula naming one interval-valued variable,
\code{~ iv(lo, hi)}.}

\item{data}{a data frame.}

\item{options}{a named list of further TDA options, passed through.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}

\item{max_boxes, max_iter, tol_width, tol_fd, tol_fe}{for
\code{tda_ivariance} only, the branch-and-bound search's limits
and tolerances (\code{nbox=}/\code{mxit=}/\code{tolbw=}/
\code{tolfd=}/\code{tolfe=}). \code{tda_imean} and
\code{tda_igini} take no such options.}

\item{x}{for \code{tda_igini}, the points at which the Lorenz curve is
evaluated.}
}
\value{
An object carrying the bounds in \code{$bounds}, and the run.
}
\description{
The interval-valued counterparts of the ordinary summaries: a mean, a
variance and a Gini coefficient computed from \code{(lower, upper)} pairs
rather than from points. Each is reported as bounds, because an interval
variable does not have a single value for these either.
}
\examples{
d <- data.frame(lo = c(1, 3, 5, 9), hi = c(2, 4, 8, 12))
tda_imean(~ iv(lo, hi), d)
tda_ivariance(~ iv(lo, hi), d)
tda_igini(~ iv(lo, hi), d)
}
\seealso{
Other interval-valued data:
\code{\link[=tda_bounds]{tda_bounds()}},
\code{\link[=tda_idf]{tda_idf()}},
\code{\link[=tda_ilsreg]{tda_ilsreg()}},
\code{\link[=tda_imreg]{tda_imreg()}},
\code{\link[=tda_inpreg]{tda_inpreg()}},
\code{\link[=tda_ivar1]{tda_ivar1()}},
\code{\link[=tda_ivreg]{tda_ivreg()}},
\code{\link[=tda_sddf]{tda_sddf()}}
}
\concept{interval-valued data}
