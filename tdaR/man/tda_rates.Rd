% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate-methods.R
\name{tda_rates}
\alias{tda_rates}
\title{The estimated rate as a single table}
\usage{
tda_rates(x)
}
\arguments{
\item{x}{a fit from \code{\link{tda_rate}} or \code{\link{tda_coxph}}.}
}
\value{
A data frame with \code{group}, \code{time}, \code{rate},
  \code{survivor} and \code{density}, or \code{NULL} if the fit carries no
  rate table. For a Cox fit \code{rate} is the baseline rate at each
  event time and \code{cumrate} the cumulative baseline rate beside
  it (already cumulative: TDA's CumRate column).
}
\description{
The companion to \code{\link{tda_survivor}}: the rate a model implies,
with the same column names whichever model produced it. \code{prate =} at
fit time is what asks TDA to compute it.
}
\examples{
d <- tda_rrdat()
f <- tda_rate(Surv(TFP, DES) ~ 1, d, prate = "0(10)300")
head(tda_rates(f))
}
\seealso{
Other rate models:
\code{\link[=tda_constrain]{tda_constrain()}},
\code{\link[=tda_control]{tda_control()}},
\code{\link[=tda_dple]{tda_dple()}},
\code{\link[=tda_split]{tda_split()}},
\code{\link[=tda_survivor]{tda_survivor()}},
\code{\link[=tda_transitions]{tda_transitions()}},
\code{\link[=vcov.tda_fit]{vcov.tda_fit()}}
}
\concept{rate models}
