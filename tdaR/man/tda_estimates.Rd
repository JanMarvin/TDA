% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate.R, R/tda.R
\name{tda_output}
\alias{tda_output}
\alias{tda_estimates}
\title{Estimates and raw output}
\usage{
tda_output(x)

tda_estimates(x)
}
\arguments{
\item{x}{a run or fitted model.}
}
\value{
A data frame, a list of data frames, or \code{NULL}.
}
\description{
\code{tda_estimates} returns the coefficient table, reading the column
layout from TDA's header so that it works for the rate models
(SN/Org/Des/MT), \code{qreg} (Cat/Term) and \code{fml} (Parameter/Value)
alike. A parameter TDA held fixed, printed as \code{---}, comes back
\code{NA}. \code{tda_output} prints everything TDA wrote.
}
\examples{
set.seed(38)
d <- data.frame(x = rnorm(100))
d$y <- rbinom(100, 1, plogis(0.2 + 0.7 * d$x))
f <- tda_qreg(y ~ x, d)
tda_estimates(f)
tda_output(f)  # everything TDA printed, not just the coefficient table
}
\seealso{
Other TDA infrastructure:
\code{\link[=tda_ccnt]{tda_ccnt()}},
\code{\link[=tda_help]{tda_help()}},
\code{\link[=tda_read_table]{tda_read_table()}},
\code{\link[=tda_run]{tda_run()}},
\code{\link[=tda_write_data]{tda_write_data()}}
}
\concept{TDA infrastructure}
