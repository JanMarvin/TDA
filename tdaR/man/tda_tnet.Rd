% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-analysis.R
\name{tda_tnet}
\alias{tda_tnet}
\title{Temporal-network summaries}
\usage{
tda_tnet(
  edges,
  report = c("basic", "subgraphs", "indegrees", "outdegrees", "layers"),
  ...
)
}
\arguments{
\item{edges}{data frame: from, to, then at least three value
columns in the order VALIDITY (>= 0 keeps the edge), then the
edge's valid-from and valid-to dates with from <= to --
established from the source's check; the command requires a
multigraph with three or more subgraphs.}

\item{report}{"basic", "subgraphs", "indegrees", "outdegrees", or
"layers".}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
the printed output.
}
\description{
TDA's \code{tnet} on a directed edge list.
}
\examples{
r <- tda_tnet(data.frame(i = c(1, 2, 3), j = c(2, 3, 4),
                         valid = c(1, 1, 1),
                         from = c(10, 12, 14), to = c(20, 22, 24)))
cat(head(tda_payload(r), 12), sep = "\n")
}
