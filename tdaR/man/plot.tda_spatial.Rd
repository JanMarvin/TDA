% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{plot.tda_spatial}
\alias{plot.tda_spatial}
\title{Draw spatial data}
\usage{
\method{plot}{tda_spatial}(
  x,
  xlim = NULL,
  ylim = NULL,
  width = 100,
  height = NULL,
  newpage = TRUE,
  what = c("objects", "hull", "hull_per_polygon"),
  symbol = NULL,
  size = NULL,
  lty = NULL,
  lw = NULL,
  gray = NULL,
  ...
)
}
\arguments{
\item{x}{a \code{\link{tda_spatial}}.}

\item{xlim, ylim}{ranges of the coordinate system.}

\item{width, height}{size of the plotting area in millimetres; the
height follows the data's aspect ratio unless given.}

\item{newpage}{start a new page before drawing.}

\item{what}{what to draw: \code{"objects"} (default), the convex hull
over every point, or the convex hull of each polygon separately;
\code{sdplot}'s \code{opt=}.}

\item{symbol, size, lty, lw, gray}{marker symbol and size, line type, line
width in mm, and grey level from 0 (black) to 1 (white); \code{sdplot}'s
own \code{s=}/\code{fs=}/\code{lt=}/\code{lw=}/\code{gs=}.}

\item{...}{further options for \code{sdplot}.}
}
\value{
The parsed drawing operations, invisibly.
}
\description{
\code{sdplot} needs a PostScript coordinate system as well as the spatial
data, so the ranges have to be given.
}
\details{
Points need a marker or nothing is drawn: give \code{symbol} (or another
TDA symbol number). This is the same trap noted for
\code{\link{tda_sd_plot3}} -- without a marker or a \code{zvar} attribute,
\code{sdplot} draws nothing at all rather than erroring.
}
\examples{
d <- data.frame(id = 1:4, x = c(0, 1, 1, 0), y = c(0, 0, 1, 1))
s <- tda_spatial(d)
plot(s, symbol = 1)  # a marker is needed; points draw nothing without one
}
\seealso{
Other spatial analysis:
\code{\link[=tda_map]{tda_map()}},
\code{\link[=tda_polygons]{tda_polygons()}},
\code{\link[=tda_read_dbf]{tda_read_dbf()}},
\code{\link[=tda_read_shapefile]{tda_read_shapefile()}},
\code{\link{tda_read_spatial}},
\code{\link[=tda_sd]{tda_sd()}},
\code{\link{tda_sd_analyses}},
\code{\link[=tda_sd_ps3]{tda_sd_ps3()}},
\code{\link[=tda_spatial]{tda_spatial()}}
}
\concept{spatial analysis}
