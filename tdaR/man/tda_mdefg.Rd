% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-analysis.R
\name{tda_mdefg}
\alias{tda_mdefg}
\title{Adjacency matrix of a graph}
\usage{
tda_mdefg(edges, missing = 0, directed = FALSE, valued = TRUE, ...)
}
\arguments{
\item{edges}{edge list (data frame with from, to and optionally a
value column).}

\item{missing}{value written where no edge exists.}

\item{directed, valued}{graph type flags, as in \code{\link{tda_g}}.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
the numeric adjacency matrix.
}
\description{
TDA's \code{mdefg}: builds the adjacency matrix of the current
graph, with \code{missing} substituted for absent edges (TDA's
\code{sc=}, default -1 there; 0 here, which gives the usual
weighted adjacency matrix).
}
\examples{
e <- data.frame(from = c(1, 1, 2), to = c(2, 3, 3),
                value = c(5, 2, 7))
tda_mdefg(e)
}
