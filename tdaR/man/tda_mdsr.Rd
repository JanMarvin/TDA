% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scaling.R
\name{tda_mdsr}
\alias{tda_mdsr}
\title{Axes in an MDS configuration}
\usage{
tda_mdsr(config, x, ...)
}
\arguments{
\item{config}{two-column matrix, the configuration.}

\item{x}{matrix of external variables (same row count).}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
the printed output.
}
\description{
TDA's \code{mdsr}: regresses external variables onto a
two-dimensional configuration to draw interpretable axes.
}
\examples{
cfg <- cbind(c(0, 1, 2, 3), c(0, 1, 0, 1))
r <- tda_mdsr(cfg, x = cbind(v = c(1, 2, 3, 4)))
cat(head(tda_payload(r), 12), sep = "\n")
}
