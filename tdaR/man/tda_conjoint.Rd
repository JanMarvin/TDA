% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{tda_conjoint}
\alias{tda_conjoint}
\title{Non-metric conjoint analysis}
\usage{
tda_conjoint(formula, data, options = list(), dir = tempfile("tda"), ...)
}
\arguments{
\item{formula}{\code{rank ~ f1 + f2}, where the response is the rank order
and the right-hand side names the factors.}

\item{data}{a data frame. Factor levels are numbered from 1 upwards, and a
value outside that range is rejected by TDA with the record it occurred
in.}

\item{options}{a named list of further TDA options, passed through.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
An object carrying the run; the diagram and the estimates are in
  \code{$run$output}.
}
\description{
\code{nmca} is conjoint measurement: it takes a rank order over profiles
and the categorical factors describing them, and estimates the part-worth
of each factor level. It draws a Hasse diagram of the order as part of the
output.
}
\examples{
d <- expand.grid(price = 1:2, brand = 1:2)
d$rank <- c(4, 2, 3, 1)
cj <- tda_conjoint(rank ~ price + brand, d)
cat(tda_payload(cj$run), sep = "\n")   # the nmca section itself
}
\seealso{
Other clustering:
\code{\link{TDA_CLUSTER}},
\code{\link{TDA_MDS}},
\code{\link[=tda_cutree]{tda_cutree()}},
\code{\link[=tda_dma]{tda_dma()}},
\code{\link[=tda_pdatd]{tda_pdatd()}}
}
\concept{clustering}
