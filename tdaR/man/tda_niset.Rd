% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{tda_niset}
\alias{tda_niset}
\title{Numerical-integration settings}
\usage{
tda_niset(rel_error = 1e-04, method = 1, commands = character(), ...)
}
\arguments{
\item{rel_error}{relative error target for the integrator.}

\item{method}{integration algorithm 1-5 (TDA's rhs of
\code{niset}), default 1, QNG.}

\item{commands}{further command lines to run under the setting.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
the printed output, invisibly.
}
\description{
TDA's \code{niset}: sets tolerance and subdivision options used by
the \code{int} operator within the same run; exposed for pipeline
use.
}
\examples{
out <- tda_niset(rel_error = 1e-6,
                 commands = "int(ab=0,1, fmt=12.8) = sin(x);")
grep("Approximation", out, value = TRUE)   # 1 - cos(1)
}
