% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{tda_dmet}
\alias{tda_dmet}
\title{Convert a distance matrix into a metric}
\usage{
tda_dmet(g, options = list(), dir = tempfile("tda"), ...)
}
\arguments{
\item{g}{a \code{\link{tda_graph}}, undirected and valued.}

\item{options}{a named list of further TDA options, passed through.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
A list: \code{original} and \code{modified}, both distance
  matrices, and \code{constant}, the value added to get from one to
  the other.
}
\description{
\code{dmet}: an undirected valued graph's edge values, read as a
distance matrix, do not always satisfy the triangle inequality (a
direct edge can be "longer" than going via a third node). This finds
the smallest constant that, added to every distance, makes the whole
matrix satisfy it -- \eqn{c = \max(0, \max_{i,j,k} d_{ij} - d_{ik} -
d_{kj})} -- and returns both the original and the corrected matrix.
}
\examples{
e <- data.frame(from = c(1, 2, 3), to = c(2, 3, 1), value = c(1, 2, 5))
g <- tda_graph(e, directed = FALSE)   # 1-3 direct (5) exceeds
                                      # 1-2-3 via node 2 (1+2=3)
tda_dmet(g)
}
\seealso{
Other graph analysis:
\code{\link[=plot.tda_graph]{plot.tda_graph()}},
\code{\link[=tda_g]{tda_g()}},
\code{\link{tda_g_analyses}},
\code{\link[=tda_graph]{tda_graph()}},
\code{\link[=tda_locate_line]{tda_locate_line()}},
\code{\link[=tda_ptree]{tda_ptree()}},
\code{\link[=tda_ucl]{tda_ucl()}}
}
\concept{graph analysis}
