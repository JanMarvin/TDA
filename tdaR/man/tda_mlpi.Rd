% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize.R
\name{tda_mlpi}
\alias{tda_mlpi}
\title{0-1 linear programming}
\usage{
tda_mlpi(
  objective,
  constraints,
  bounds,
  constant = 0,
  max_solutions = 100,
  dir = tempfile("tda"),
  ...
)
}
\arguments{
\item{objective}{integer coefficients \code{c}, one per variable (at
least two variables).}

\item{constraints}{the constraint matrix \code{A}, one row per
constraint (at least one), \code{length(objective)} columns.}

\item{bounds}{the right-hand sides \code{b}, one per constraint row.}

\item{constant}{\code{k}, added to the reported value (default 0).}

\item{max_solutions}{how many optimal solutions to return at most.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
A list: \code{value}, the maximum; \code{solutions}, a matrix
  with one optimal 0-1 vector per row.
}
\description{
Maximize a linear objective over 0-1 variables under linear
constraints -- TDA's \code{mlpi} (CACM algorithm 449). The problem
solved is
\deqn{\max\; c'x + k \quad \mathrm{s.t.}\; A x \ge b,\; x \in \{0,1\}^n}{
max c'x + k  subject to  A x >= b,  x in {0,1}^n}
with integer coefficients (negative ones are fine). The command's
source states no objective; this is what it computes, established
against exhaustive search -- see \code{examples/coverage/mlpiops.cf}.
}
\examples{
# maximize 2 x1 - 3 x2 + 4 x3 + x4  s.t.  sum(x) >= 2,
# -x1 + 2 x2 + x4 >= 1
tda_mlpi(c(2, -3, 4, 1),
        rbind(c(1, 1, 1, 1), c(-1, 2, 0, 1)),
        bounds = c(2, 1))
}
\seealso{
Other optimization:
\code{\link[=tda_boolean_min]{tda_boolean_min()}},
\code{\link[=tda_mlp]{tda_mlp()}},
\code{\link[=tda_mls]{tda_mls()}},
\code{\link[=tda_mqap]{tda_mqap()}},
\code{\link[=tda_mqp]{tda_mqp()}}
}
\concept{optimization}
