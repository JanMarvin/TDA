% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/further-regression.R
\name{tda_npreg}
\alias{tda_npreg}
\title{Non-parametric regression}
\usage{
tda_npreg(
  formula,
  data,
  x,
  method = c("mean", "quantile", "frequency", "lowess", "midmeans"),
  kernel = c("uniform", "triangle", "quartic", "epanechnikov"),
  bandwidth = NULL,
  select = NULL,
  options = list(),
  dir = tempfile("tda"),
  ...
)
}
\arguments{
\item{formula}{a two-sided formula.}

\item{data}{a data frame.}

\item{x}{for \code{tda_npreg}, the points at which to evaluate the fit.}

\item{method}{see Details; \code{npreg}'s \code{opt=}. Either a
name (partially matched) or TDA's number directly (\code{opt=1}
is \code{"mean"}, and so on) -- useful when translating a \code{.cf}
file, which only ever writes the number.}

\item{kernel}{the smoothing kernel, only meaningful for
\code{method = "mean"} -- \code{npreg}'s \code{k=}. A name or
TDA's number, the same as \code{method}.}

\item{bandwidth}{the kernel bandwidth (\code{npreg}'s \code{d=},
default 1).}

\item{select}{a case-selection expression, TDA's \code{sel=}.}

\item{options}{a named list of further TDA options, passed through.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
An object carrying the fitted values as \code{table}.
}
\description{
\code{tda_npreg} is a non-parametric regression evaluated at points you
choose: TDA's \code{npreg}, a kernel-smoothed mean, quantile or
frequency.
}
\details{
\code{method} picks which: \code{"mean"} (the default), a
kernel-smoothed mean at each point in \code{x}; \code{"quantile"},
smoothed quantiles; \code{"frequency"}, smoothed frequencies. Only
these three exist. \code{npreg} names the columns for
\code{method} \code{"mean"}
and \code{"quantile"}, which \code{tda_npreg} reproduces;
\code{"frequency"} writes columns that depend on the data (the
distinct response categories) and are returned as TDA numbered them,
\code{V1}, \code{V2}, ...
}
\examples{
set.seed(19)
d <- data.frame(x = round(rnorm(60), 3))
d$y <- round(1 + sin(d$x) + rnorm(60, sd = 0.2), 3)
np <- tda_npreg(y ~ x, d, x = seq(-2, 2, 0.5))
np$table

# a triangle kernel, wider bandwidth
np2 <- tda_npreg(y ~ x, d, x = seq(-2, 2, 0.5), kernel = "triangle",
                 bandwidth = 0.5)
np2$table
}
\seealso{
Other regression:
\code{\link{TDA_FAMILIES}},
\code{\link{TDA_QRMODELS}},
\code{\link[=tda_freg]{tda_freg()}},
\code{\link[=tda_gdf]{tda_gdf()}},
\code{\link[=tda_l1reg]{tda_l1reg()}},
\code{\link[=tda_lsreg]{tda_lsreg()}},
\code{\link[=tda_mlrc_design]{tda_mlrc_design()}},
\code{\link[=tda_mreg]{tda_mreg()}},
\code{\link[=tda_nlreg]{tda_nlreg()}},
\code{\link[=tda_zreg]{tda_zreg()}}
}
\concept{regression}
