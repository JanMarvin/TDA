% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoothing.R
\name{tda_spl}
\alias{tda_spl}
\title{Smoothing spline}
\usage{
tda_spl(
  x,
  y,
  weights = NULL,
  sig = 1,
  deg = NULL,
  max = 200,
  rx = NULL,
  options = list(),
  dir = tempfile("tda")
)
}
\arguments{
\item{x, y}{the pair of variables.}

\item{weights}{optional per-point weights, the same idea as
\code{weights} on the fitting functions.}

\item{sig}{smoothing factor; 0 interpolates.}

\item{deg}{degree of the spline.}

\item{max}{maximum number of knots.}

\item{rx}{range over which to evaluate, e.g. \code{seq(0, 10, 1)}.}

\item{options}{a named list of further TDA options, passed through.}

\item{dir}{working directory.}
}
\value{
An object carrying a \code{table} of fitted values.
}
\description{
A smoothing spline through a pair of variables, optionally weighted.
\code{smooth.spline} is the R analogue.
}
\examples{
set.seed(33)
x <- sort(runif(30, 0, 10))
y <- sin(x) + rnorm(30, sd = 0.15)
sp <- tda_spl(x, y, sig = 1)
head(sp$table)
}
\seealso{
Other smoothing:
\code{\link[=tda_integrate]{tda_integrate()}},
\code{\link[=tda_interp]{tda_interp()}},
\code{\link[=tda_isotonic]{tda_isotonic()}},
\code{\link[=tda_mat]{tda_mat()}},
\code{\link[=tda_sma]{tda_sma()}},
\code{\link[=tda_smd]{tda_smd()}}
}
\concept{smoothing}
