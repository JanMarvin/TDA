% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tda.R
\name{tda_ccnt}
\alias{tda_ccnt}
\alias{tda_lcnt}
\alias{tda_dump}
\alias{tda_dsplit}
\title{Inspect a raw file}
\usage{
tda_ccnt(file, dir = tempfile("tda"), ...)

tda_lcnt(file, noc = NULL, dir = tempfile("tda"), ...)

tda_dump(file, nc = NULL, offset = NULL, dir = tempfile("tda"), ...)

tda_dsplit(file, len = NULL, dir = tempfile("tda"), ...)
}
\arguments{
\item{file}{path to the file to inspect.}

\item{dir}{working directory; for \code{tda_dsplit}, also where the
parts are written, alongside the original file.}

\item{...}{passed to \code{\link{tda_run}}.}

\item{noc}{for \code{tda_lcnt}, the maximum number of records to read.}

\item{nc}{for \code{tda_dump}, how many bytes to show; all of them by
default.}

\item{offset}{for \code{tda_dump}, where to start, in bytes.}

\item{len}{for \code{tda_dsplit}, the size of each part in bytes.}
}
\value{
\code{tda_ccnt} and \code{tda_lcnt} return a data frame.
  \code{tda_dump} returns the hex dump as a character vector, one line
  each. \code{tda_dsplit} returns the paths of the parts it wrote.
}
\description{
Small file-diagnostic utilities from TDA's toolkit, from an era
before assuming \code{wc}, \code{od} or \code{split} were available:
\code{tda_ccnt} a character frequency table, \code{tda_lcnt} a
record-length frequency table, \code{tda_dump} a hex dump, and
\code{tda_dsplit} splits a file into fixed-size parts on disk.
}
\examples{
f <- tempfile()
writeLines(c("hello world", "foo bar baz", "x"), f)
tda_ccnt(f)
tda_lcnt(f)
cat(tda_dump(f), sep = "\n")

f2 <- tempfile()
writeLines(rep("0123456789", 10), f2)
tda_dsplit(f2, len = 20)
}
\seealso{
Other TDA infrastructure:
\code{\link[=tda_help]{tda_help()}},
\code{\link[=tda_output]{tda_output()}},
\code{\link[=tda_read_table]{tda_read_table()}},
\code{\link[=tda_run]{tda_run()}},
\code{\link[=tda_write_data]{tda_write_data()}}
}
\concept{TDA infrastructure}
