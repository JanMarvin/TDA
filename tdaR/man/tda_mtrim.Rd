% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix.R
\name{tda_mtrim}
\alias{tda_mtrim}
\title{Trim or pad a matrix's edges}
\usage{
tda_mtrim(
  A,
  leading_cols = 0,
  leading_rows = 0,
  trailing_cols = 0,
  trailing_rows = 0,
  dir = tempfile("tda")
)
}
\arguments{
\item{A}{a numeric matrix.}

\item{leading_cols, trailing_cols, leading_rows, trailing_rows}{integer counts (default 0); positive deletes, negative pads
with zeros.}

\item{dir}{working directory.}
}
\value{
A numeric matrix.
}
\description{
Deletes or adds whole rows/columns at the edges of \code{A} --
TDA's \code{mtrim}. A positive count deletes that many rows or
columns from that edge; a negative count adds that many zero
rows/columns instead. \code{leading_cols}/\code{trailing_cols}
act on the first and last columns, \code{leading_rows}/
\code{trailing_rows} on the first and last rows.
}
\examples{
A <- matrix(1:4, 2)
tda_mtrim(A, leading_cols = 1)    # drop the first column
tda_mtrim(A, leading_cols = -1)   # add a zero column in front
}
\seealso{
Other matrix reshaping:
\code{\link[=tda_mag]{tda_mag()}},
\code{\link[=tda_mcath]{tda_mcath()}},
\code{\link[=tda_mcvec]{tda_mcvec()}},
\code{\link[=tda_mrsum]{tda_mrsum()}},
\code{\link[=tda_msort]{tda_msort()}},
\code{\link[=tda_msrow]{tda_msrow()}}
}
\concept{matrix reshaping}
