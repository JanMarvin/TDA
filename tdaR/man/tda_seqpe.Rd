% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequence.R
\name{tda_seqpe}
\alias{tda_seqpe}
\title{Derive a sequence data structure from episode data}
\usage{
tda_seqpe(
  data,
  id,
  origin,
  destination,
  start,
  end,
  tp,
  missing = NULL,
  missing_start = NULL,
  missing_end = NULL,
  options = list(),
  dir = tempfile("tda"),
  ...
)
}
\arguments{
\item{data}{a data frame of episodes, one row per spell.}

\item{id}{name of the case identifier column in \code{data}.}

\item{origin, destination}{names of the origin- and destination-state
columns.}

\item{start, end}{names of the start- and end-time columns.}

\item{tp}{definition of the time points the sequence is built over, as
a vector or a single TDA range expression such as \code{"0(1)10"}.}

\item{missing}{value to use for a gap at the beginning
(\code{missing_start}), at the end (\code{missing_end}), or anywhere
else (\code{missing}); TDA's default is \code{-1} for all three.}

\item{missing_start, missing_end}{see \code{missing}.}

\item{options}{a named list of further TDA options, passed through.}

\item{dir}{working directory.}

\item{...}{passed to \code{\link{tda_run}}.}
}
\value{
A data frame: the case id, then one column per time point in
  \code{tp}, each holding the state the case was in at that time.
}
\description{
\code{seqpe}: builds a sequence -- a state at every time point -- from
episode data (a start time, an end time, and origin/destination states
for each spell). The result can be read back with a fresh call to
\code{\link{tda_seqm}} or any of the other sequence functions once
written out and re-read; this function only performs the conversion,
since TDA's output is a plain data file rather than something kept
in memory across calls the way \code{\link{tda_seqm}} manages
internally.
}
\examples{
# two cases, two spells each
e <- data.frame(id = c(1, 1, 2, 2), org = c(1, 2, 1, 3),
                des = c(2, 3, 3, 1), ts = c(0, 3, 0, 2),
                tf = c(3, 5, 2, 5))
tda_seqpe(e, id = "id", origin = "org", destination = "des",
         start = "ts", end = "tf", tp = "0(1)5")
}
\seealso{
Other sequence analysis:
\code{\link[=tda_evalfi]{tda_evalfi()}},
\code{\link[=tda_fml]{tda_fml()}},
\code{\link[=tda_frml]{tda_frml()}},
\code{\link[=tda_seq_info]{tda_seq_info()}},
\code{\link[=tda_seqgc]{tda_seqgc()}},
\code{\link[=tda_seqm]{tda_seqm()}},
\code{\link[=tda_seqmd]{tda_seqmd()}},
\code{\link[=tda_seqpm]{tda_seqpm()}}
}
\concept{sequence analysis}
