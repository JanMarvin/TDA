# gd15.cf

nvar(
    dfile = gd4.dat,
    I = c1,
    J = c2,
    V = c3,
);
gdd(gt=2) = I,J,V;

psfile = gd15.ps;
psetup(
    pxa = 0,1,
    pya = 0,1,
    pxlen = 90,
    pylen = 50,
);
pltree(
    rt = 9,     # root
    pl = 3,
    nc = 100,
);
